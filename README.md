# Containers

- CNC container with docker socket access provides some sort of API layer to the docker socket + networking with the central server to make the computer accessible.

- Spawnable celery worker container
- Spawnable ollama container
